
FROM line 1250 down is what I did... (see history.txt)


http://mirrors.ibiblio.org/pub/mirrors/eclipse/technology/epp/downloads/release/kepler/SR2/eclipse-cpp-kepler-SR2-linux-gtk-x86_64.tar.gz

sudo apt-get install zlib1g-dev (for z lib for delivery, missing???)

sudo adduser user vboxsf (to allow access to shared folders from host)

git reset HEAD --hard (in cmake-setup branch)

git pull origin cmake-setup

git clone https://mla133@github.com/mla133/AICB.git
git clone https://mla133@github.com/mla133/SAI_Engine.git

for Debugging

in empty build-native directory:
cmake -DCMAKE_BUILD_TYPE=Debug ../

apt-get installs:
 1276  sudo apt-get install vim
 1289  sudo apt-get install linux-headers-$(uname -r)
 1290  sudo apt-get autoremove
 1291  sudo apt-get install build-essential
 1440  sudo apt-get install zlib1g-dev
 1512  sudo apt-get install libsqlite0
 1513  sudo apt-get install sqlite
 1733  sudo apt-get install crypto++
 1739  sudo apt-get install libssl0.9.8
 1740  sudo apt-get install libssl-dev

